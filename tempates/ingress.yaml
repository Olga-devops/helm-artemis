apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: olga-artemis
  # annotations:
   # nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: {{ .Values.endpoint }}
  - http:
      paths:
      - path: /
        # pathType: Prefix
        backend:
          serviceName: olga-artemis
          servicePort: 80
